---
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';
// Supports weights 100-900
import '@fontsource-variable/onest';
import '@fontsource/geist-sans';

interface Props {
	title: string;
}

const { title } = Astro.props;

import '@/styles/globals.css'
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<!-- <meta name="generator" content={Astro.generator} /> -->
		<title>{title}</title>
	</head>
	<body>
		
		<div class="fixed top-0 z-[-2] h-screen w-screen bg-[#f4f4f6]"></div>


		<div class="wrapper py-[50px] pl-[50px] pr-[200px]">
			<div class=" fixed max-w-[250px] h-[720px] flex flex-col ">
				<Header />
				<Footer />
			</div>

		<main class="ml-[250px] w-auto py-12 px-16 ">
			<slot />
		</main>
		
		</div>
	</body>
</html>
<style is:global>
	/* body {
		font-family: 'Onest Variable', sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue';
	} */

	
</style>
