---
// Supports weights 100-900
import '@fontsource-variable/onest';
import '@fontsource/geist-sans';
import '@fontsource/chelsea-market';

// Components
import Sidebar from '@/components/Sidebar';
import RightLogo from '@/components/RightLogo';
import TestingScroll from '@/components/TestingScroll'

// Styles
import '@/styles/globals.css'
import '@/styles/scroll.css'

// Props
const { title } = Astro.props;

interface Props {
	title: string;
}

//Data
import sidebarLinks from '@/data/sidebarLinks'
import FadeAnimation from '@/components/animations/FadeAnimation';
import { ReactLenis } from "lenis/react";
import { Spotlight } from '@/components/ui/Spotlight';


---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<!-- <meta name="generator" content={Astro.generator} /> -->
		<title>{title}</title>
	</head>
	<body>
		<ReactLenis root>

			<!-- BACKGROUND -->
		    <div class="fixed inset-0 -z-10 h-screen w-screen bg-[#f4f4f6]"></div>
			<!-- <Spotlight
				client:load
                className="left-20 top-0 w-[1800px] h-[1800px] opacity-100 fixed"
                fill=" #991b1b"
            /> -->
			 <!-- <div
  class="fixed inset-0 h-full w-full bg-[#f4f4f6] bg-[radial-gradient(#bbbbbb_1px,transparent_1px)] [background-size:20px_20px]"
></div> -->
<!-- <div class="fixed h-full w-full bg-[#f4f4f6]"><div class="absolute bottom-0 left-[-20%] right-0 top-[-10%] h-[500px] w-[500px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.15),rgba(255,255,255,0))]"></div><div class="absolute bottom-0 right-[-20%] top-[-10%] h-[500px] w-[500px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.15),rgba(255,255,255,0))]"></div></div> -->
 
			<!-- WRAPPER -->
			<div class="h-screen w-screen relative">

				<!-- SIDEBAR LEFT-->
				 <Sidebar 
                 client:idle 
                 avatar="https://pbs.twimg.com/media/GSHjbB1WAAA4BVI?format=jpg&name=large"
                 links={sidebarLinks}
                 />

				<!-- CONTENT -->
			  	<main class="lg:px-6 px-4 lg:ml-[240px] xl:ml-[270px] lg:mr-[45px] xl:mr-[65px] relative pb-12 ">
					 <slot />

					 <!-- RIGHTBAR -->
					<RightLogo client:idle />
				</main>

			</div>

		</ReactLenis>
	</body>
</html>
<style is:global>
	/* body {
		font-family: 'Onest Variable', sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue';
	} */
</style>

